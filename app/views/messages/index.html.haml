.wrapper
  = render "top_bar"
  .message
    .col.s6
      #tab1.card
        .blue-grey.darken-1
          %ul.card
            - @results.each do |result|
              = render @message
  = render "bottom_bar"
